function multiplicationTable(num) {

    for(let i = 1; i <= 10;i++) {
        let currentTimes = i;
        console.log(`${num} X ${currentTimes} = ${num * currentTimes}`);
    }
}
multiplicationTable(5);
  
